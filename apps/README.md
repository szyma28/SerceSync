# Applications

This directory contains the implementation surfaces of SerceSync:
- `mobile/` for the Flutter care staff app
- `web/` for the Flutter manager web app
- `api/` for the NestJS backend API
